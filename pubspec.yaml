name: empty
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter

  flutter_svg: ^2.2.2
  provider: ^6.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  pedometer: ^4.0.0  # Assegura't d'usar la versió més recent
  permission_handler: ^11.0.0 # Per gestionar permisos fàcilment

flutter:
  uses-material-design: true

  assets:
    - assets/images/davidtop.jpg
    - assets/diceroller/
